# NOTE: Do NOT commit real secrets to version control.
# Create this secret manually before deploying:
#
#   kubectl create secret generic postgres-secret \
#     --from-literal=username=admin \
#     --from-literal=password=<your-strong-password> \
#     -n infinite-ai
#
# The template below uses placeholder base64 values (admin / changeme).
# Generate your own:  echo -n 'value' | base64
#
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: infinite-ai
type: Opaque
data:
  # echo -n 'admin' | base64
  username: YWRtaW4=
  # echo -n 'changeme' | base64  — REPLACE BEFORE USE
  password: Y2hhbmdlbWU=
